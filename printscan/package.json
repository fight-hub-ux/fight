{
  "name": "printscan",
  "version": "1.0.0",
  "description": "Print price comparison tool for Express Print Services Ltd",
  "main": "server/server.js",
  "scripts": {
    "start": "node server/server.js",
    "dev": "nodemon server/server.js",
    "client": "cd client && npm start",
    "install:all": "npm install && cd client && npm install",
    "setup": "npx playwright install chromium",
    "build": "cd client && npm run build"
  },
  "dependencies": {
    "express": "^4.18.2",
    "playwright": "^1.40.0",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "ws": "^8.14.2",
    "better-sqlite3": "^9.2.2",
    "crypto-js": "^4.2.0",
    "@anthropic-ai/sdk": "^0.20.1",
    "node-fetch": "^2.7.0",
    "uuid": "^9.0.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
